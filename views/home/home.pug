extends ../_layout

block content
    h1 Welcome to Drones Universe
    .fullscreen-bg
        video.fullscreen-bg__video(
            loop muted autoplay
        )
            source(src="/static/videos/DatabasesEF&CloudDBs.mp4"
                type = "video/mp4")
